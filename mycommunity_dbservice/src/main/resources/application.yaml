server:
  port: 9000

mybatis:
  configuration:
    map-underscore-to-camel-case: true
    # ¿ØÖÆÌ¨
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:mappers/*.xml

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mycommunity?characterEncoding=utf-8&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: jsbbsj
    username: root

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
